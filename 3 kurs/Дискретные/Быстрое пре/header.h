#include <iostream>
#include <vector>
#include <math.h>
#include <bitset>

using namespace std;

const int n = 4;
const int l = pow(2, n);

vector<float> BPF(vector<float> f, vector<vector<float>> B);
